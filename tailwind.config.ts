// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        emerald: '#059669', // Primary Brand (Emerald Green)
        kelly: '#4ade80',   // Success/Action (Kelly Green)
        gold: '#FBBF24',    // Highlights/Value Tags (Warm Gold)
        charcoal: '#1F2937',// Rich Text
        silver: '#F3F4F6',  // Backgrounds
      }
    }
  }
}